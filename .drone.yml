pipeline:
  install:
    image: node:10.9.0
    commands:
      # - yarn install
      - pwd


  # testing:
  #   image: node:10.9.0
  #   group: testing
  #   commands:
  #     - yarn run test
  
  scp:
    image: appleboy/drone-scp
    pull: true
    host:
      - 139.162.9.68
    port: 22
    username: root
    user: root
    secrets: [ ssh_password ]
    target: /root
    source:
      - /drone/src/github.com/horsekitlin/dinbandan
    when:
      branch: develop

  ssh:
    image: appleboy/drone-ssh
    host:
      - 139.162.9.68
    username: root
    user: root
    secrets: [ ssh_password ]
    command_timeout: 600
    script:
      - . /root/.nvm/nvm.sh && nvm use 10.9.0
      - mkdir -p /var/www/html/dinbandan.tomaslin.website
      - cd /root/drone/src/github.com/horsekitlin/dinbandan/
      - yarn install
      - yarn build
      - cp -a build/* /var/www/html/dinbandan.tomaslin.website
      - cd /root
      - rm -rf /root/drone
    when:
      branch: develop